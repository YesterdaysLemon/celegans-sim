# The niche museum has a public building

The catalogue lives at [`web/museum.md`](../web/museum.md) now, so the site itself can
serve it — the viewer renders it at `/museum.html`, one door from either dish. This
pointer stays because links to the old address exist in commit history and in the
wild; the accession rules, the wings and every exhibit are all in the new building,
unchanged.
